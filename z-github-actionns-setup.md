GitHub Repo
  └── Settings (tab)
      └── Secrets and variables (sidebar)
          └── Actions
              └── New repository secret ← Click here

Secret Name	        Where to Get It
OKE_CLUSTER_OCID	OCI Console → Kubernetes → Cluster → Copy OCID
OCI_REGION	        e.g., ap-mumbai-1, us-ashburn-1
OCI_TENANCY_OCID	OCI Console → Profile → Tenancy → Copy OCID
OCI_USER_OCID	    OCI Console → Profile → User → Copy OCID
OCI_FINGERPRINT	    OCI Console → Profile → API Keys → Fingerprint
OCI_PRIVATE_KEY	    The .pem file content from y